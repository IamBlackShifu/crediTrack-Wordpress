<?php
defined( 'WP_UNINSTALL_PLUGIN' ) || exit;
// Financial data is deliberately retained. Deletion requires a future explicit,
// separately confirmed administrator opt-in; uninstalling is never that consent.
